info:
  name: /api/informationPackages/{IPID}
  type: http
  seq: 2

http:
  method: GET
  url: https://{{ZONTAL_HOST}}/api/informationPackages/{{IPID}}
  auth:
    type: bearer
    token: "{{zontal_access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
