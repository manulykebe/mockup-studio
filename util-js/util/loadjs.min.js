'use strict'; var loadJSList = loadJSList || []; (function (w) { var requestId; var loadJSReady = function (f) { if (loadJSList.filter(function (r) { return r.s == 0 }).length > 0) requestAnimationFrame(function () { loadJSReady(f) }); else { if (f !== undefined && typeof f == 'function') f() } }; var addOnLoad = function (o, f) { var _f = o.onload; if (typeof o.onload != 'function') { o.onload = f } else { o.onload = function () { if (_f) { _f() } f() } } }; var reLoadJS = async function (src, callback, async) { loadJSList.splice(loadJSList.findIndex(function (l) { return l.src === src }), 1); return await loadJS(src, callback, async) }; var loadJS = async function (src, callback, async, b) { "use strict"; b = b || false; let regexmin = /\.min\.js$/; let regexn = /\.js$/; if (typeof (callback) === 'boolean') { var tmp = async; async = callback; callback = tmp } var callCallback = function (err, result) { if (typeof callback === 'function') { try { callback(err, result) } catch (ignored) { } } }; if (location.hostname === 'localhost' && !b) src = src.replace(regexmin, '.js'); if (location.hostname !== 'localhost' && !b) src = src.replace(regexn, '.min.js').replace('.min.min.', '.min.'); var normalizedSrc = src.replace(regexmin, '.js'); var found = loadJSList.find(x => x.src === normalizedSrc); if (found) { if (found.s == -1) { var err = new Error('Failed to load script: ' + src); callCallback(err); return Promise.reject(err) } if (found.s === 1) { callCallback(null, found.script); return Promise.resolve(found.script) } if (found.promise) { found.promise.then(function (script) { callCallback(null, script) }).catch(callCallback); return found.promise } } var delimiter = (src.indexOf('?') >= 0) ? '&' : '?'; var _src = src + (document.location.host === 'localhost' ? '' : (delimiter + '_=' + Date.now())); var fl = { el: 'script', t: 'text/javascript', l: 'src' }; if ((src.split('?').reverse()[0].split('.').reverse()[0]) == 'css') fl = { el: 'link', t: 'text/css', l: 'href' }; var _hdscrpt = w.document.getElementsByTagName('head')[0].getElementsByTagName('script'); var ref = _hdscrpt[_hdscrpt.length - 1]; var script = w.document.createElement(fl.el); script.type = fl.t; script[fl.l] = _src; if (fl.el === 'link') script.rel = 'stylesheet'; var _id = Math.random().toString(36).replace(/[^a-z0-9]+/g, ''); script.id = _id; script.async = !async; ref.parentNode.insertBefore(script, ref.nextSibling); var resolvePromise, rejectPromise; var promise = new Promise(function (resolve, reject) { resolvePromise = resolve; rejectPromise = reject }); loadJSList.push({ id: script.id, src: normalizedSrc, s: 0, start: performance.now(), checkpoints: [1000, 2000, 5000, 5000], promise: promise, resolve: resolvePromise, reject: rejectPromise, script: script }); addOnLoad(script, function () { var i = loadJSList.findIndex(x => x.id === _id); if (i > -1) { loadJSList[i].s = 1; loadJSList[i].duration = parseInt((performance.now() - loadJSList[i].start) * 10) / 10; delete loadJSList[i].start; loadJSList[i].resolve(script); document.getElementById(_id).remove() } }); if (typeof callback === 'function') { promise.then(function (script) { callCallback(null, script) }).catch(callCallback) } var loop = function (time) { var slowRunningScripts = loadJSList.filter(lSL => lSL.s === 0 && Array.isArray(lSL.checkpoints) && lSL.checkpoints.length > 0).filter(lJL => (performance.now() - lJL.start) > lJL.checkpoints[0]); if (slowRunningScripts.length > 0) { slowRunningScripts.forEach(sls => { var shift = true; if (sls.checkpoints.length === 2) shift = false; var i; if (shift) i = sls.checkpoints.shift(); else i = sls.checkpoints[0]; console.log('slow script loading detected ...' + (i / 1000) + 's...: ' + sls.src + '.'); if (!shift) sls.checkpoints[0] += sls.checkpoints[1] }) } if (loadJSList.filter(x => x.s === 0).length > 0) requestId = requestAnimationFrame(loop) }; loop(); script.onerror = function () { if (src.match(regexmin) && !b) { document.getElementById(_id).remove(); console.warn('fallback', 'will try to load the non-minified script of ' + src); loadJSList.splice(loadJSList.findIndex(x => x.src === normalizedSrc), 1); var __src = src.replace(regexmin, '.js'); loadJS(__src, callback, async, true).then(resolvePromise).catch(rejectPromise); return } if (src.match(regexn) && !b) { document.getElementById(_id).remove(); console.warn('warning', 'will try to load the minified script of ' + src); loadJSList.splice(loadJSList.findIndex(x => x.src === normalizedSrc), 1); var __src = src.replace(regexn, '.min.js'); loadJS(__src, callback, async, true).then(resolvePromise).catch(rejectPromise); return } document.getElementById(_id).remove(); loadJSList.filter(x => x.src === normalizedSrc).forEach(e => { if (typeof e.reject === 'function') { e.reject(new Error('Failed to load script: ' + src)) } _.merge(e, { s: -1, start: undefined }) }); var err = new Error('kon benodigde script niet laden!\n' + src); callCallback(err); return }; return promise }; if (typeof module !== 'undefined') { module.exports = { loadJS: loadJS, reLoadJS: reLoadJS, loadJSReady: loadJSReady } } else { w.loadJS = loadJS; w.reLoadJS = reLoadJS; w.loadJSReady = loadJSReady } })(typeof global !== 'undefined' ? global : this);
